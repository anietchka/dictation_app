# Files in the config/locales directory are used for internationalization and
# are automatically loaded by Rails. If you want to use locales other than
# English, add the necessary files in this directory.
#
# To use the locales, use `I18n.t`:
#
#     I18n.t "hello"
#
# In views, this is aliased to just `t`:
#
#     <%= t("hello") %>
#
# To use a different locale, set it with `I18n.locale`:
#
#     I18n.locale = :es
#
# This would use the information in config/locales/es.yml.
#
# To learn more about the API, please read the Rails Internationalization guide
# at https://guides.rubyonrails.org/i18n.html.
#
# Be aware that YAML interprets the following case-insensitive strings as
# booleans: `true`, `false`, `on`, `off`, `yes`, `no`. Therefore, these strings
# must be quoted to be interpreted as strings. For example:
#
#     en:
#       "yes": yup
#       enabled: "ON"

en:
  hello: "Hello world"
  application:
    title: "Dictation App"
    welcome: "Welcome to Dictation App"
    logged_in_as: "You are logged in as:"
  sessions:
    require_login: "You must be logged in to access this page"
    login_success: "Login successful"
    logout_success: "Logout successful"
    invalid_credentials: "Invalid email or password"
    email: "Email"
    password: "Password"
    login: "Log in"
    logout: "Log out"
    no_account: "Don't have an account yet?"
    signup_link: "Create an account"
  users:
    create_success: "Account created successfully"
    email: "Email"
    password: "Password"
    password_confirmation: "Password confirmation"
    password_minimum: "Minimum 8 characters"
    signup: "Create account"
    already_have_account: "Already have an account?"
    login_link: "Log in"
  activerecord:
    errors:
      models:
        user:
          attributes:
            email:
              blank: "Email can't be blank"
              taken: "This email is already taken"
              invalid: "Email is invalid"
            password:
              too_short: "Password must contain at least 8 characters"
              blank: "Password can't be blank"
        dictation:
          attributes:
            user:
              required: "User is required"
            min_words:
              blank: "Minimum number of words can't be blank"
              not_a_number: "Minimum number of words must be a number"
              not_an_integer: "Minimum number of words must be an integer"
              greater_than: "Minimum number of words must be greater than 0"
            max_words:
              blank: "Maximum number of words can't be blank"
              not_a_number: "Maximum number of words must be a number"
              not_an_integer: "Maximum number of words must be an integer"
              greater_than: "Maximum number of words must be greater than 0"
              must_be_greater_than_min_words: "Maximum number of words must be greater than or equal to minimum number of words"
